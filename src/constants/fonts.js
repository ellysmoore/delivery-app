import metrics from './metrics';

const Fonts = {
  h1: metrics.screenWidth * (32 / 375),
  h2: metrics.screenWidth * (28 / 375),
  h3: metrics.screenWidth * (24 / 375),
  h4: metrics.screenWidth * (20 / 375),
  p1: metrics.screenWidth * (16 / 375),
  p2: metrics.screenWidth * (14 / 375),
  p3: metrics.screenWidth * (12 / 375),
};

export default Fonts;
